server:
  port: 8081

spring:
  application:
    name: consul-provider

  cloud:
    consul:
      host: 127.0.0.1
      port: 8500

  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-active: 80
        max-wait: 2000s
        max-idle: 30
        min-idle: 10